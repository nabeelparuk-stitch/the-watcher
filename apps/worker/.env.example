# Same project URL; service role key from `supabase status` (server only — never ship to the browser)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=

# Local Temporal dev server defaults
TEMPORAL_ADDRESS=127.0.0.1:7233
TEMPORAL_NAMESPACE=default
TEMPORAL_SCHEDULE_ID=fleet-probe-sweep

# Optional label stored on each probe row
PROBE_REGION=local
